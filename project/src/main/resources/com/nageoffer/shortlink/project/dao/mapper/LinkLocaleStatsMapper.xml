<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nageoffer.shortlink.project.dao.mapper.LinkLocaleStatsMapper">


    <insert id="shortLinkStats" parameterType="com.nageoffer.shortlink.project.dao.entity.LinkLocaleStatsDO">
        INSERT INTO
        t_link_locale_stats (
        full_short_url,
        gid,
        date,
        cnt,
        province,
        city,
        adcode,
        country,
        create_time,
        update_time,
        del_flag
        )
        VALUES (
        #{fullShortUrl},
        #{gid},
        #{date},
        #{cnt},
        #{province},
        #{city},
        #{adcode},
        #{country},
        #{createTime},
        #{updateTime},
        #{delFlag}
        )
        ON DUPLICATE KEY UPDATE
        cnt = cnt + #{cnt},
        update_time = #{updateTime};
    </insert>
    <select id="getOneLinkLocaleCntBetweenDateGroupByProvince"
            resultType="com.nageoffer.shortlink.project.dto.resp.ShortLinkStatsLocaleCNRespDTO">
        SELECT
        full_short_url,
        SUM(cnt) AS cnt
        FROM
        t_link_locale_stats
        WHERE
        gid = #{gid}
        AND full_short_url = #{fullShortUrl}
        AND del_flag = 0
        AND date BETWEEN #{startDate} AND #{endDate}
        GROUP BY
        full_short_url, gid, province
    </select>


</mapper>

