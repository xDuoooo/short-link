<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xduo.shortlink.project.dao.mapper.LinkMapper">
    <update id="incrementStats">
        update t_link set total_pv = total_pv + #{totalPv}, total_uv = total_uv + #{totalUv}, total_uip = total_uip +
        #{totalUip} where gid = #{gid} and full_short_url = #{fullShortUrl}
    </update>

    <select id="listGroupShortLinkCount"
            resultType="com.xduo.shortlink.project.dto.resp.ShortLinkGroupCountQueryRespDTO">
        select gid,count(1) as shortLinkCount FROM t_link where 
        <if test="list != null and list.size() > 0">
            gid in
            <foreach collection="list" open="(" close=")" separator="," item="each">
                #{each}
            </foreach>
            and 
        </if>
        enable_status=1 and del_flag = 0
        group by gid
    </select>

    <!-- 分页查询短链接 - 优化版本 -->
    <select id="pageLink" parameterType="com.xduo.shortlink.project.dto.req.ShortLinkPageReqDTO"
resultType="com.xduo.shortlink.project.dao.entity.LinkDO">
        SELECT t.*,
        COALESCE(s.today_pv, 0) AS todayPv,
        COALESCE(s.today_uv, 0) AS todayUv,
        COALESCE(s.today_uip, 0) AS todayUip
        FROM t_link t
        LEFT JOIN t_link_stats_today s ON t.gid = s.gid
        AND t.full_short_url = s.full_short_url
        AND s.date = CURDATE()
    WHERE t.del_flag = 0
        <choose>
            <when test="includeRecycle != null and includeRecycle == true">
                <!-- 包含回收站：显示所有状态的短链接 -->
            </when>
            <otherwise>
                <!-- 不包含回收站：只显示启用状态的短链接 -->
                AND t.enable_status = 1
            </otherwise>
        </choose>
        <if test="gid != null and gid != ''">
            AND t.gid = #{gid}
        </if>
        <choose>
            <when test="orderTag == 'todayPv'">
                ORDER BY todayPv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'todayUv'">
                ORDER BY todayUv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'todayUip'">
                ORDER BY todayUip DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalPv'">
                ORDER BY t.total_pv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalUv'">
                ORDER BY t.total_uv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalUip'">
                ORDER BY t.total_uip DESC, t.create_time DESC
            </when>
            <otherwise>
                ORDER BY t.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- 优化的分页查询短链接 - 当有gid过滤时使用 -->
    <select id="pageLinkOptimized" parameterType="com.xduo.shortlink.project.dto.req.ShortLinkPageReqDTO"
resultType="com.xduo.shortlink.project.dao.entity.LinkDO">
        SELECT t.*,
        COALESCE(s.today_pv, 0) AS todayPv,
        COALESCE(s.today_uv, 0) AS todayUv,
        COALESCE(s.today_uip, 0) AS todayUip
        FROM t_link t
        LEFT JOIN t_link_stats_today s ON t.gid = s.gid
        AND t.full_short_url = s.full_short_url
        AND s.date = CURDATE()
    WHERE t.del_flag = 0
        <choose>
            <when test="includeRecycle != null and includeRecycle == true">
                <!-- 包含回收站：显示所有状态的短链接 -->
            </when>
            <otherwise>
                <!-- 不包含回收站：只显示启用状态的短链接 -->
                AND t.enable_status = 1
            </otherwise>
        </choose>
        <if test="gid != null and gid != ''">
            AND t.gid = #{gid}
        </if>
        <choose>
            <when test="orderTag == 'todayPv'">
                ORDER BY todayPv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'todayUv'">
                ORDER BY todayUv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'todayUip'">
                ORDER BY todayUip DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalPv'">
                ORDER BY t.total_pv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalUv'">
                ORDER BY t.total_uv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalUip'">
                ORDER BY t.total_uip DESC, t.create_time DESC
            </when>
            <otherwise>
                ORDER BY t.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- 批量分页查询短链接 - 支持多个分组批量查询 -->
    <select id="batchPageLinkOptimized" parameterType="com.xduo.shortlink.project.dto.req.ShortLinkBatchPageReqDTO"
    resultType="com.xduo.shortlink.project.dao.entity.LinkDO">
        SELECT t.*,
        COALESCE(s.today_pv, 0) AS todayPv,
        COALESCE(s.today_uv, 0) AS todayUv,
        COALESCE(s.today_uip, 0) AS todayUip
        FROM t_link t
        LEFT JOIN t_link_stats_today s ON t.gid = s.gid
        AND t.full_short_url = s.full_short_url
        AND s.date = CURDATE()
        WHERE t.del_flag = 0
        AND t.enable_status = 1
        <if test="gids != null and gids.size() > 0">
            AND t.gid IN
            <foreach collection="gids" open="(" close=")" separator="," item="gid">
                #{gid}
            </foreach>
        </if>
        <choose>
            <when test="orderTag == 'todayPv'">
                ORDER BY todayPv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'todayUv'">
                ORDER BY todayUv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'todayUip'">
                ORDER BY todayUip DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalPv'">
                ORDER BY t.total_pv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalUv'">
                ORDER BY t.total_uv DESC, t.create_time DESC
            </when>
            <when test="orderTag == 'totalUip'">
                ORDER BY t.total_uip DESC, t.create_time DESC
            </when>
            <otherwise>
                ORDER BY t.create_time DESC
            </otherwise>
        </choose>
        LIMIT #{size}
    </select>

    <!-- 批量分页查询回收站短链接 - 支持多个分组批量查询，避免全表扫描 -->
    <select id="pageRecycleBinLinkOptimized" parameterType="com.xduo.shortlink.project.dto.req.ShortLinkRecycleBinPageReqDTO"
    resultType="com.xduo.shortlink.project.dao.entity.LinkDO">
        SELECT *
        FROM t_link
        WHERE del_flag = 0
        AND enable_status = 0
        <if test="gidList != null and gidList.size() > 0">
            AND gid IN
            <foreach collection="gidList" open="(" close=")" separator="," item="gid">
                #{gid}
            </foreach>
        </if>
        ORDER BY create_time DESC
    </select>
</mapper>
