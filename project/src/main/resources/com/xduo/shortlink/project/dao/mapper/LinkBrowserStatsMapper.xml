<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xduo.shortlink.project.dao.mapper.LinkBrowserStatsMapper">


    <insert id="shortLinkStats" parameterType="com.xduo.shortlink.project.dao.entity.LinkBrowserStatsDO">
        INSERT INTO
        t_link_browser_stats (
        full_short_url,
        gid,
        date,
        cnt,
        browser,
        create_time,
        update_time,
        del_flag
        )
        VALUES (
        #{fullShortUrl},
        #{gid},
        #{date},
        #{cnt},
        #{browser},
        #{createTime},
        #{updateTime},
        #{delFlag}
        )
        ON DUPLICATE KEY UPDATE
        cnt = cnt + #{cnt},
        update_time = #{updateTime};
    </insert>
    <select id="listBrowserStatsByShortLink" resultType="com.xduo.shortlink.project.dao.entity.LinkBrowserStatsDO">
        SELECT
        browser,
        SUM(cnt) AS cnt
        FROM
        t_link_browser_stats
        WHERE
        full_short_url = #{requestParam.fullShortUrl}
        AND gid = #{requestParam.gid}
        AND date BETWEEN #{requestParam.startDate} AND #{requestParam.endDate}
        GROUP BY
        browser
    </select>
    <select id="listGroupBrowserStatsByShortLink"
            resultType="com.xduo.shortlink.project.dao.entity.LinkBrowserStatsDO">
        SELECT
        browser,
        SUM(cnt) AS cnt
        FROM
        t_link_browser_stats
        WHERE
        gid = #{requestParam.gid}
        AND date BETWEEN #{requestParam.startDate} AND #{requestParam.endDate}
        GROUP BY
        browser

    </select>


</mapper>

